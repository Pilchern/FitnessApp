# Test Workspace

- `e2e/`: browser and route-level tests
- `fixtures/`: shared seed fixtures and provider payload samples

Layer-specific unit and integration tests will be added alongside the packages they verify.
